eval:
  n_perturbations: 0
  seed: 42

benchmark:
  path: consistent_agents.benchmarks.harbor_spider2_dbt:HarborSpider2DBTBenchmark
  params:
    split: "all"
    cache_dir: .cache
    tasks_root: .harbor_tasks/spider2_dbt_codex_header_shuffle
    force_reclone: false
    overwrite: false
    include_db_variants: true
    db_perturb_seed: 42
    db_perturb_spec:
      name: header_shuffle
      perturbation_types:
        - header_shuffle

perturbations: []

harbor:
  n_concurrent: 1
  task:
    template_path: .harbor_tasks/spider2_dbt_codex_header_shuffle
    instruction_file: instruction.md
    workdir: .harbor_runs
    keep: false
    rewrite_instruction: false
  agent:
    name: codex
    model_name: openai/gpt-5-mini
  environment:
    type: docker
    delete: true
  verifier: {}
  trials_dir: harbor-trials
  timeout_multiplier: 1.0
  reward_key: reward

output:
  path: outputs/harbor-spider2-dbt-codex-gpt5mini-header-shuffle
